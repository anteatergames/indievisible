var MODULETEMPLATE = (function () {
    "use strict";

    function init() {
        bindAll();
    }

    function bindAll() {
        //bindSave();
    }

    function bindSave() {
    }


    return {
        Init: init
    };
}());


$(function () {
    MODULETEMPLATE.Init();
});