var MODULETEMPLATE = (function () {
    "use strict";

    function init() {
        bindAll();
    }

    function bindAll() {
    }


    return {
        Init: init
    };
}());


$(function () {
    MODULETEMPLATE.Init();
});