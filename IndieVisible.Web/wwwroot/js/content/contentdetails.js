var CONTENTDETAILS = (function () {
    "use strict";

    var selectors = {};


    function init() {
        cacheSelectors();

        bindAll();
    }

    function cacheSelectors() {
    }

    function bindAll() {
    }

    return {
        Init: init
    };
}());


$(function () {
    CONTENTDETAILS.Init();
});