var CONTENTDETAILS = (function () {
    "use strict";

    function init() {
        cacheSelectors();

        bindAll();
    }

    function cacheSelectors() {
    }

    function bindAll() {
    }

    return {
        Init: init
    };
}());


$(function () {
    CONTENTDETAILS.Init();
});